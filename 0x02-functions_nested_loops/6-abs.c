#include "main.h"
/**
 * _abs - function prints the absolute value abs
 * @n: is int for the input number
 * Return: always 0 (Success)
 *
 */
int _abs(int n)
{
	int y;

	if (n >= 0)
	{
		_putchar(n);
	}
	else
	{
		y = -1 * n
		_putchar(y);
	}
	return (0);
}
