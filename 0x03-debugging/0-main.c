#include "main.h"
/**
 * main - Debug positive_or_negative() function,
 * test case when input equal to 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(test_value);
	return (0);
}
