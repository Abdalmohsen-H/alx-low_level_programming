CC = gcc
SRC = main.c school.c
OBJ = $(SRC:.c=.o) # this makes a list of .o files based on names of .c files on SRC by replacing .c with .o while keeping file names and save it OBJ variable
NAME = school
CFLAGS = -Wall -Werror -Wextra -pedantic
# add phony targets to avoid issues where a file named the same as a target in the the same folder, os this will allow Make file to work without any issue
.PHONY: all clean oclean fclean re
all: m.h ${OBJ}
	${CC} ${OBJ} -o $(NAME)
# we didn't declare R.M variable because it already defined as builtin in make , so we don't need to overwrite its behaviour
# this meets requirements, also you culd use make -p command to see builtin defined variables that you could overwrite or use them directly
# same goes for CFLAGS it is a builtin in make file so it knows when and how to use ,it knows it used for (compiling .c files to .o).
clean:
	${RM} *~ .*.swp ${NAME} # remove tmp files auto generated by emacs *~ and vim .*.swp also may use .*.sw? if vim use other extensions like .*.swm or
#	.*.swo, also this cmd # deletes executable file school
oclean:
	$(RM) $(OBJ)
fclean : clean oclean
re : oclean all
